# 6.UDP广播实现

# 6.1 广播介绍

TCP通信采用一对一的通信模式。日常生活中的网络会议通知、广告、网络信息公告等功能，需要采用UDP实现一对多的群发功能。

通过Internet实现群发功能的形式有两种：

(1)利用广播向子网中的所有客户发送消息，比如各类通知、单位公告、集体活动日程安排等；

(2)利用组播向Internet网上不同的子网发送消息，比如集团向其所属的公司或用户子网发布信息公告等。

广播顾名思义，就是想对局域网内所有的主机同时发送数据，但是广播还是要指明接收者的端口号的，因为不可能接受者的所有端口都来收听广播。

广播IP地址：192.168.1.255/192.168.1.(-1)

广播 MAC 地址: 255.255.255.255

# 广播的特点是

子网中未参加相应广播应用的所有主机都会受到广播影响向一个广播地址发送 UDP 数据报时所发生的步骤为:

首先假设子网地址为192.168.34,则该子网的广播地址为192.168.34.255,此子网下有俩台IP地址分别为192.168.34.1和192.168.34.2的主机

(1)广播时sendto向IP为192.168.34.255端口为3333发送数据报，此IP数据报在以太网下会激活ARP将目的广播地址192.168.34.255映射成相应的以太网地址(物理地址)，然后将目的地址为此地址的帧发出去

(2)这次 IP 为 192.168.34.1 和 192.168.34.2 的主机接口都会和此广播地址匹配成功, 直到该数据报进入 UDP 层时根据端口是否匹配才判断是否丢弃该数据, 端口为 3333 的会保留该数据,否则将数据丢弃

# 总结

广播：根据定义，广播分组会去往子网上的所有主机，包括发送主机自身，广播的缺点：未参加广播的主机也会受到影响

# 2. 网络连接模型

# 单播模型

单播的特点为：单播IP数据报仅由通过目的IP地址指定的单个主机接收，子网上的其他主机都不会受到任何影响

向一个单播地址发送 UDP 数据报时所发生的步骤为:

首先假设子网地址为192.168.34,此子网下有俩台IP地址分别为192.168.34.1和192.168.34.2的主机

(1)假设sendto往IP为192.168.34.2端口为3333发送数据报，此IP数据报在以太网下会激活ARP将目的以太网地址(192.168.34.2)映射成相应的以太网地址(物理地址)，然后将目的地址为此地址的帧发出去

(2)在 192.168.34 子网下的主机的以太网接口看到该帧的时候都会与自己的以太网地址对比,如果不一致, 该接口就会忽略该帧(也就是说单播不会对主机造成任何的额外开销, 因为忽略他们的是接口而不是主机)

(3)直到 IP 为 192.168.34.2 的主机看到该帧时，比较发现相同，该接口读入整个帧，IP 层处理他时会比较目的 IP 地址和自身 IP 地址，同样 UDP 层会检查目的端口和该进程端口，随后会将数据报置于相应的接收队列，这就是 UDP 单播的全过程

# 6.2 广播编模模型

![image](https://cdn-mineru.openxlab.org.cn/result/2026-03-21/d7132512-d678-4b51-9363-daf95f8afa7f/eae8de05d49ef2a8083e266dd73ab69ee287576455d4a765afc601996e7e3adc.jpg)



广州小蚁智控科技有限公司


# 开发流程

1）创建socket套接字

```txt
sockfd = socket(AF_INET, SOCK_DGRAM, 0) 
```

2）允许发送广播数据包

```txt
int on = 1; 
```

```txt
setsockopt(ockfd, SOL_SOCKET, SO_BROADCAST, &on, sizeof(on)) 
```

3）绑定广播地址 192.168.1.255

```txt
bind() 
```

4）发送广播数据包

```txt
sendto() 
```

5）接收广播数据包

```txt
recvfrom() 
```

6）关闭套接字

```javascript
close(ockfd); 
```

# 6.3 广播编程API

setsockopt函数

在前面的网络编程中，所使用的socket属性均为默认属性，如果将socket属性进行特殊设置，就可以让socket工作于特殊状态，例如允许端口重用、发送广播信息等等。

头文件

```txt
include <sys/types.h> 
```

```txt
include <sys/socket.h> 
```

原型

```c
int setsockopt(int sockfd, int level, int optname, const void *optval, socklen_t optlen); 
```

参数说明

Sockfd: 套接字描述符

```txt
Level: 指定控制套接字属性的层次
```

SOL_SOCKET: 通用套接字选项

IPPROTO_IP: IP选项

IPPROTO_TCP: TCP选项

Optname: 指定控制参数，是相对上面属性的层次来说的

比如上面 SOL_SOCKET 的选项可以有：

SO_BROADCAST: 允许发送广播地址

SO_TYPE 获得套接字类型

SO_RCVTIMEO 接收超时

SO_SNDTIMEO 接收超时

Optval: 设置套接字选项值, 决定选择的属性是否开启, 1-开启,0-关闭

Optlen: 缓冲区大小

# 示例讲解

![image](https://cdn-mineru.openxlab.org.cn/result/2026-03-21/d7132512-d678-4b51-9363-daf95f8afa7f/d2bcaae59ad746bd7ca8c86c45ec068fd5af700650114c898af9dc8b2158a194.jpg)


# 要求：

1. 发送端 IP 地址为 192.168.1.100，处于同一个局域网的两台接收端的 IP 地址分别为 192.168.1.200/192.168.1.300

2. 发送端以广的方式发送数据包出，本身主机能否收到？

3.接收端192.168.1.200主机能否收到？

4.接收端192.168.1.300主机能否收到？

recv接收设计

```c
include <sys/types.h>   
#include <sys/ Socket.h>   
#include<stdio.h>   
#include <unistd.h>   
#include <netinet/in.h>   
#include <arpa/inet.h>   
#include<stdlib.h>   
#define MAX_RDBUF_LEN 1024   
int main(void)   
{ int recv_fd,ret,rd_cnt; struct sockaddr_in recv_addr,send_addr; socklen_t recv_len, send_len; char rd_buf [MAX_RDBUF_LEN]; 
```

```c
recv_fd = socket(AF_INET, SOCK_DGRAM, 0);  
if(recv_fd == -1)  
{  
    perror("socket fail");  
    exit(1);  
}  
recv_addr.sin_family = AF_INET;  
recv_addr.sin_port = htons(8090);  
recv_addr.sin_addr.s_addr = htons(INADDR_ANY);  
recv_len = sizeof(recv_addr);  
ret = bind(recv_fd, (struct sockaddr *)&recv_addr, recv_len);  
if(ret == -1)  
{  
    perror("bind fail");  
    exit(2);  
}  
while(1)  
{rd_cnt = recvfrom(recv_fd, rd_buf, MAX_RDBUF_LEN, 0, &send_addr, &send_len);  
if(rd_cnt == -1)  
{  
    perror("receive client data fail");  
    exit(3);  
}  
printf("received client:%s data:%s\n", inet_ntoa send_addr.sin_addr), rd_buf);  
}  
close(recv_fd);  
return 0; 
```

send 发送设计

include <sys/types.h> 

include <sys/socket.h> 

include<stdio.h> 

include <unistd.h> 

include <netinet/in.h> 

include<stdio.h> 

include <arpa/inet.h> 

include<stdio.h> 

```c
#define MAXBuf_CNT 1024
int main(void)
{
    int send_fd, ret, wr_cnt;
    struct sockaddr_in broadcast_addr;
    socklen_t broadcast_len;
    char send_buf [MAXBuf_CNT];
    char recv_buf [MAXBuf_CNT];
    send_fd = socket(AF_INET, SOCK_DGRAM, 0);
    if (send_fd == -1)
        {
            perror("socket fail");
            exit(1);
        }
    int on = 1;
    setsockopt (send_fd, SOL_SOCKET, SO_BROADCAST, &on, sizeof(on));
    broadcast_addr.sin_family = AF_INET;
    broadcast_addr.sin_port = htons(8090);
    broadcast_addr.sin_addr.s_addr = inlet_addr("192.168.0.255");
    broadcast_len = sizeof(struct sockaddr_in);
    memset (send_buf, 0, MAXBuf_CNT);
    strcpy (send_buf, "hi, broadcast information.");
    wr_cnt = sendto (send_fd, send_buf, strlen (send_buf) + 1, 0, (struct sockaddr *)&broadcast_addr,
                broadcast_len);
    if (wr_cnt == -1)
        {
            perror("sendto fail");
            exit(2);
        }
    close (send_fd);
    return 0;
} 
```